int sumDigits(int n) {
    int sum = 0;
    while (n > 0) {
        sum += n % 10;
        n /= 10;
    }
    return sum;
}

Complexity of this code is O(LOG N) because complexity grows only when n grows 10 times